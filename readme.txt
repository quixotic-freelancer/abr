#################################################################
Install python (anything above 3.7.4 should work)

Open terminal.

Run the following lines:

pip install -r requirements.txt
python swag.py
#################################################################
Customize terrain details in terrain_rules.txt
Set size of generated map in swag.py (current size is 1000x1000)
#################################################################
